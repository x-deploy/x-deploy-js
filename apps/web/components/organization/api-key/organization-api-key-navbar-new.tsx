import {Button} from "../../basics/buttons";

export const OrganizationApiKeyNavbarNew = ({ organizationId }: { organizationId: string }) => {
    return (
        <div>
            <Button variant={"primary"}>New Key</Button>
        </div>
    )
}
